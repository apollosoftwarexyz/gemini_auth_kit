name: gemini_auth_kit
description: Authentication kit for Gemii

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ">=2.17.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  mockito:
  flutter_bloc:
  get_it:
  dio:
  json_annotation: ^4.4.0


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  build_runner:
  json_serializable: ^6.2.0

flutter:
